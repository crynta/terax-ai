import type { Theme } from "../types";

export const termaxDefault: Theme = {
  id: "termax-default",
  name: "TerMax Default",
  description: "The default TerMax look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
